{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the tracemux shell window.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-execute"
  ]
}
