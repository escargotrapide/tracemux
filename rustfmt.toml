edition = "2021"
max_width = 100
newline_style = "Unix"
use_field_init_shorthand = true
# NOTE: nightly-only options (`imports_granularity`, `group_imports`) are
# applied via `cargo +nightly fmt` in CI. They are intentionally not
# included here so stable `cargo fmt` doesn't print warnings.

